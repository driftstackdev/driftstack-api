{
  "annotations": { "list": [] },
  "description": "Arc 4 Wave 2.B sub-slice 8.22 — pair-mode state machine observability. Source counter: driftstack_pair_mode_transition_total{from,to}.",
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": { "defaults": { "unit": "ops" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "options": { "legend": { "displayMode": "table", "placement": "right" } },
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
          "expr": "sum by (to) (rate(driftstack_pair_mode_transition_total[5m]))",
          "legendFormat": "→ {{to}}",
          "refId": "A"
        }
      ],
      "title": "Pair-mode transitions per second (by target state)",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": { "defaults": { "unit": "percentunit" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "options": { "legend": { "displayMode": "list", "placement": "bottom" } },
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
          "expr": "sum(rate(driftstack_pair_mode_transition_total{to=\"takeover-queued\"}[10m])) / sum(rate(driftstack_pair_mode_transition_total{to=~\"takeover-pending|takeover-queued\"}[10m]))",
          "legendFormat": "takeover-queued / total",
          "refId": "A"
        }
      ],
      "title": "Takeover queue ratio (queued / queued+pending)",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": { "defaults": { "unit": "ops" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 },
      "id": 3,
      "options": { "legend": { "displayMode": "list", "placement": "bottom" } },
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
          "expr": "sum(rate(driftstack_pair_mode_transition_total{to=\"ai-driving\",from!=\"ai-driving\"}[5m]))",
          "legendFormat": "auto-handback rate",
          "refId": "A"
        }
      ],
      "title": "Heartbeat-timeout auto-handback rate",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 },
      "id": 4,
      "options": {},
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
          "expr": "sum by (from, to) (driftstack_pair_mode_transition_total)",
          "format": "table",
          "instant": true,
          "refId": "A"
        }
      ],
      "title": "All-time transition totals (from → to)",
      "type": "table"
    }
  ],
  "refresh": "30s",
  "schemaVersion": 38,
  "tags": ["driftstack", "pair-mode", "v2-#8"],
  "templating": {
    "list": [
      {
        "current": { "selected": false, "text": "Prometheus", "value": "Prometheus" },
        "hide": 0,
        "includeAll": false,
        "label": "Datasource",
        "multi": false,
        "name": "DS_PROMETHEUS",
        "options": [],
        "query": "prometheus",
        "queryValue": "",
        "refresh": 1,
        "regex": "",
        "skipUrlSync": false,
        "type": "datasource"
      }
    ]
  },
  "time": { "from": "now-6h", "to": "now" },
  "timepicker": {},
  "timezone": "utc",
  "title": "Driftstack — Pair-mode state machine",
  "uid": "driftstack-pair-mode",
  "version": 1,
  "weekStart": "monday"
}
