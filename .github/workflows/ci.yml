name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-test:
    name: Build, typecheck, lint, unit + integration tests
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:17-alpine
        env:
          POSTGRES_USER: driftstack
          POSTGRES_PASSWORD: driftstack
          POSTGRES_DB: driftstack
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U driftstack -d driftstack"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

    env:
      NODE_ENV: test
      DATABASE_URL: postgres://driftstack:driftstack@localhost:5432/driftstack
      REDIS_URL: redis://localhost:6379
      DRIVER: mock

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node 22
        uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install
        run: npm ci

      # SDK examples import from `@driftstack/sdk`, which resolves through
      # the workspace symlink to packages/sdk-typescript/dist/. Building the
      # SDK before typecheck ensures dist/ exists when the examples are
      # checked. Same goes for the workspace build of api-types — the SDK
      # depends on its compiled output.
      - name: Build (api-types + sdk + server)
        run: npm run build

      - name: Build SDK (tsup ESM + CJS + .d.ts)
        run: npm run build --workspace packages/sdk-typescript

      - name: Typecheck
        run: npm run typecheck

      - name: Lint
        run: npm run lint

      - name: Format check
        run: npm run format:check

      - name: Test (unit + integration) with coverage thresholds
        # V-107: vitest.config.ts enforces coverage thresholds (lines,
        # statements, functions, branches). CI fails if coverage drops
        # below the regression gate set in V-107.
        run: npx vitest run --coverage

  e2e:
    name: End-to-end (Playwright against real Postgres + Redis)
    needs: build-test
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:17-alpine
        env:
          POSTGRES_USER: driftstack
          POSTGRES_PASSWORD: driftstack
          POSTGRES_DB: driftstack
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U driftstack -d driftstack"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

    env:
      NODE_ENV: test
      DATABASE_URL: postgres://driftstack:driftstack@localhost:5432/driftstack
      REDIS_URL: redis://localhost:6379
      DRIVER: mock
      CI: 'true'

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node 22
        uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install
        run: npm ci

      - name: Build
        run: npm run build

      - name: Run e2e suite
        working-directory: apps/server
        run: npm run test:e2e

      - name: Upload Playwright HTML report
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: playwright-report
          path: apps/server/playwright-report/
          retention-days: 7

  python-sdk:
    name: Python SDK (lint + tests)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.10'
          cache: 'pip'
          cache-dependency-path: 'packages/sdk-python/pyproject.toml'

      - name: Install package + dev deps
        working-directory: packages/sdk-python
        run: |
          python -m pip install --upgrade pip
          pip install -e '.[dev]'

      - name: Ruff check
        working-directory: packages/sdk-python
        run: ruff check .

      - name: Ruff format check
        working-directory: packages/sdk-python
        run: ruff format --check .

      - name: Mypy (strict on hand-written code)
        working-directory: packages/sdk-python
        run: mypy src

      - name: Pytest
        working-directory: packages/sdk-python
        run: pytest -v

      - name: Build wheel + sdist
        working-directory: packages/sdk-python
        run: |
          pip install build
          python -m build

      - name: Smoke-test wheel install in a fresh venv
        working-directory: packages/sdk-python
        run: |
          python -m venv /tmp/smoke
          /tmp/smoke/bin/pip install dist/driftstack-*.whl
          /tmp/smoke/bin/python -c "
          from driftstack import Driftstack, AsyncDriftstack, verify_webhook_signature
          from driftstack import DriftstackError, RateLimitError, AuthError
          from driftstack._generated.models import Session, ApiKey, WebhookEndpoint
          client = Driftstack(api_key='ds_test_aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa')
          # Original 4 resources
          assert hasattr(client, 'sessions')
          assert hasattr(client, 'api_keys')
          assert hasattr(client, 'usage')
          assert hasattr(client, 'webhooks')
          # V-103 additions
          assert hasattr(client, 'profiles')
          assert hasattr(client, 'billing')
          assert hasattr(client, 'auth')
          # Async parity
          aclient = AsyncDriftstack(api_key='ds_test_aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa')
          for accessor in ['sessions', 'api_keys', 'usage', 'webhooks', 'profiles', 'billing', 'auth']:
              assert hasattr(aclient, accessor), accessor
          print('wheel smoke ok — all 7 resource accessors wired')
          "

  go-sdk:
    name: Go SDK (vet + tests + examples build)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.22'
          cache: false # no go.sum yet — zero non-stdlib runtime deps

      - name: go vet
        working-directory: packages/sdk-go
        run: go vet ./...

      - name: go test
        working-directory: packages/sdk-go
        run: go test -v ./...

      - name: Build examples
        working-directory: packages/sdk-go
        run: go build ./examples/...
