node_modules/
dist/
build/
coverage/
*.tsbuildinfo
package-lock.json
# pnpm lockfile + workspace manifest. Mirrors the package-lock.json line above:
# a lockfile is generated, nobody hand-edits it, and reformatting one produces a
# large meaningless diff. Without this the format gate goes red the moment a
# pnpm lockfile exists in the tree — on a file no one is supposed to touch.
pnpm-lock.yaml
pnpm-workspace.yaml
LICENSE

# Drizzle generated artifacts
apps/server/src/db/migrations/

# Astro generated artifacts
apps/marketing-site/.astro/
apps/marketing-site/dist/
apps/customer-dashboard/.astro/
apps/customer-dashboard/dist/
apps/admin-panel/.astro/
apps/admin-panel/dist/
apps/docs/.astro/
apps/docs/dist/
apps/status-site/.astro/
apps/status-site/dist/

# Frozen verification-log archive. Append-only history, never hand-edited again, and large
# enough that Prettier needs more heap than the pre-commit hook gives it. Same rationale as the
# lockfiles above: reformatting produces a large meaningless diff on a file nobody should touch.
docs/verification-log-archive-through-v1200.md
docs/verification-log-archive-through-v1499.md
#
# ⛔ Each archive is listed LITERALLY, and a glob here would be a silent mistake.
# `no-formatted-markdown-outgrows-the-format-hook` keeps this list honest by reading
# it, and its matcher is literal (`rel === p || rel.endsWith(p)`) — so a glob would
# satisfy Prettier while leaving that guard red on a hook that was in fact fine.
# One line per archive, added by hand at each split.
