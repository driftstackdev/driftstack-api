{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "updater-windows-linux",
  "description": "Updater install and relaunch permissions for the main Windows and Linux app. macOS carries the same grant in updater-check-macos.json as of 2026-09-01; it was excluded before that to protect a code requirement the adhoc-signed build does not actually have (see that file for the codesign/spctl measurement).",
  "windows": ["main"],
  "permissions": ["updater:default", "process:default"],
  "platforms": ["windows", "linux"]
}
