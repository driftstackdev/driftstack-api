{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "simulator-app",
  "description": "Least-privilege capability for the SEPARATE 'Driftstack Simulator' macOS app build (tauri.simulator.conf.json). That build's window keeps the label `main` (the Rust `--ds-session` handoff at lib.rs uses get_webview_window(\"main\")), so this capability targets `main`. The separate-app build sets app.security.capabilities=[\"simulator-app\"] in its config, which OVERRIDES the default auto-inclusion of capabilities/*.json — so the separate app gets ONLY this set and NEVER the broad `default` capability (audit 1.8 least-privilege). Deliberately EXCLUDES shell / updater / process / deep-link / create-webview-window — none are reachable from the simulator entry (main.tsx ?window=simulator → RecordingsProvider + SimulatorWindow). The grants below are exactly what the simulator window + the libs it imports actually use:\n  • window mutation: start-dragging (frame drag), close + destroy (X button + onCloseRequested teardown), minimize, set-size (rotate/resize), set-always-on-top (pin), set-focus.\n  • core:window:default — read-only window queries.\n  • core:webview:default — getCurrentWebviewWindow + onCloseRequested.\n  • core:event:default — listen('ds-session') relaunch re-point.\n  • core / app / resources — baseline IPC.\n  • store:default — lib/settings.ts LazyStore get/set/save in loadSettings().\n  • fs (read/write text, mkdir, remove, exists, read-dir) scoped to $APPDATA/recordings/** — RecordingsProvider.loadIndex() runs on mount; the Record pill persists frames there.\n  • fs:scope $DOWNLOAD/** + fs:allow-write-file — the snapshot button writes a PNG to Downloads.\nApp-defined commands (secret_load/secret_save/secret_delete via loadSettings, sim_key_take, set_dock_tile/reset_dock_tile for the dynamic macOS Dock badge) need NO ACL entry in Tauri v2 — locally-registered commands are always callable; only plugin/core permissions are listed here.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:app:default",
    "core:resources:default",
    "core:window:default",
    "core:window:allow-start-dragging",
    "core:window:allow-set-focus",
    "core:window:allow-close",
    "core:window:allow-destroy",
    "core:window:allow-minimize",
    "core:window:allow-set-size",
    "core:window:allow-set-always-on-top",
    "core:webview:default",
    "core:event:default",
    "store:default",
    {
      "identifier": "fs:scope",
      "allow": [{ "path": "$APPDATA/recordings/**" }, { "path": "$DOWNLOAD/**" }]
    },
    "fs:allow-read-text-file",
    "fs:allow-write-text-file",
    "fs:allow-write-file",
    "fs:allow-remove",
    "fs:allow-mkdir",
    "fs:allow-exists",
    "fs:allow-read-dir"
  ]
}
