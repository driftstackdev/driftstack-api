---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout
  title="Docs"
  description="Driftstack documentation: quickstart, SDK references for TypeScript / Python / Go, API reference, self-hosted setup, recipe library."
>
  <!-- Header -->
  <section class="bg-gradient-to-b from-surface-base to-surface-raised">
    <div class="mx-auto max-w-6xl px-6 py-20">
      <p class="font-mono text-xs uppercase tracking-widest text-glow-red">Docs</p>
      <h1 class="mt-3 text-4xl font-semibold tracking-tight text-ink-primary md:text-5xl">
        Everything you need to drive a session.
      </h1>
      <p class="mt-6 max-w-prose text-lg text-ink-secondary">
        Quickstarts for every supported language, the full API reference,
        SDK guides, self-hosted setup, and the recipe catalogue. Driftstack
        is small enough to read end-to-end — start with the quickstart
        and follow the link trail.
      </p>
    </div>
  </section>

  <!-- Doc categories -->
  <section class="border-t border-white/10 bg-surface-raised">
    <div class="mx-auto max-w-6xl px-6 py-16">
      <div class="grid gap-6 md:grid-cols-3">
        <a
          href="/docs/api-quickstart"
          class="group rounded-lg border border-white/10 bg-surface-raised p-6 shadow-sm transition hover:border-oxblood-700"
        >
          <p class="font-mono text-xs uppercase tracking-widest text-glow-red">
            Quickstart
          </p>
          <h2 class="mt-3 text-xl font-semibold text-ink-primary group-hover:text-glow-red">
            Five minutes to first session →
          </h2>
          <p class="mt-3 text-sm text-ink-secondary">
            Install the SDK in your language, paste an API key, drive
            your first session. No prior browser-automation experience
            needed.
          </p>
        </a>

        <a
          href="/api-reference"
          class="group rounded-lg border border-white/10 bg-surface-raised p-6 shadow-sm transition hover:border-oxblood-700"
        >
          <p class="font-mono text-xs uppercase tracking-widest text-glow-red">
            API reference
          </p>
          <h2 class="mt-3 text-xl font-semibold text-ink-primary group-hover:text-glow-red">
            Every endpoint, every shape →
          </h2>
          <p class="mt-3 text-sm text-ink-secondary">
            Auto-generated from the OpenAPI 3.1 spec. Try requests
            against your own API key directly in the browser. Same
            spec the SDK codegen reads.
          </p>
        </a>

        <a
          href="/docs/sdk-typescript"
          class="group rounded-lg border border-white/10 bg-surface-raised p-6 shadow-sm transition hover:border-oxblood-700"
        >
          <p class="font-mono text-xs uppercase tracking-widest text-glow-red">SDKs</p>
          <h2 class="mt-3 text-xl font-semibold text-ink-primary group-hover:text-glow-red">
            TypeScript · Python · Go →
          </h2>
          <p class="mt-3 text-sm text-ink-secondary">
            Native packages on npm, PyPI, and as a Go module. Typed
            request/response shapes. Retry-with-backoff. Webhook
            signature verification. Async iterators for paginated
            list endpoints.
          </p>
        </a>

        <a
          href="/docs/webhooks"
          class="group rounded-lg border border-white/10 bg-surface-raised p-6 shadow-sm transition hover:border-oxblood-700"
        >
          <p class="font-mono text-xs uppercase tracking-widest text-glow-red">Webhooks</p>
          <h2 class="mt-3 text-xl font-semibold text-ink-primary group-hover:text-glow-red">
            Event types + signature verification →
          </h2>
          <p class="mt-3 text-sm text-ink-secondary">
            All five event types — session.completed, session.failed,
            api_key.revoked, quota.warning_80pct, quota.exceeded — with
            payload shapes and HMAC-SHA256 verification examples.
          </p>
        </a>

        <a
          href="/self-hosted"
          class="group rounded-lg border border-white/10 bg-surface-raised p-6 shadow-sm transition hover:border-oxblood-700"
        >
          <p class="font-mono text-xs uppercase tracking-widest text-glow-red">
            Self-hosted
          </p>
          <h2 class="mt-3 text-xl font-semibold text-ink-primary group-hover:text-glow-red">
            Deploy on your own hardware →
          </h2>
          <p class="mt-3 text-sm text-ink-secondary">
            SKU comparison, hardware specs, onboarding flow. For
            sustained high-concurrency operations or full data
            sovereignty.
          </p>
        </a>

        {/* W210 — the Recipe library card pointed at /docs/recipes,
            which doesn't exist yet on the marketing-site /docs/* mirror.
            The recipe library IS shipped at v1.0 in its write-only form
            (slice 121 promoted it to the roadmap NOW section, and the
            apps/docs/src/pages/api/recipes.md page is live at
            docs.driftstack.dev/api/recipes/). The card is omitted until
            a /docs/recipes mirror page lands on the marketing-site, so
            this stays a card-less anchor rather than a card linking to
            an external host (which would break the all-internal-anchors
            pattern the other cards on this hub follow). */}
      </div>
    </div>
  </section>

  <!-- Help banner -->
  <section class="border-t border-white/10 bg-surface-base">
    <div class="mx-auto max-w-6xl px-6 py-12">
      <div class="flex flex-col items-start gap-4 md:flex-row md:items-center md:justify-between">
        <div>
          <h2 class="text-xl font-semibold text-ink-primary">Doc not landing?</h2>
          <p class="mt-2 max-w-prose text-sm text-ink-secondary">
            Email <a
              href="mailto:support@driftstack.dev"
              class="text-glow-red underline">support@driftstack.dev</a> with the URL you expected to find. We answer in writing,
            usually same business day.
          </p>
        </div>
        <a href="https://github.com/driftstackdev" class="btn-secondary">GitHub →</a>
      </div>
    </div>
  </section>
</BaseLayout>
