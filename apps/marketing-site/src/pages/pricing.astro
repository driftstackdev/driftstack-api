---
import BaseLayout from '../layouts/BaseLayout.astro';
import {
  API_TIERS,
  SELF_HOSTED_ARCHETYPE_UPDATES,
  SELF_HOSTED_SKUS,
  SELF_HOSTED_SOFTWARE_UPDATES,
  SELF_HOSTED_SOURCE_ACCESS,
  TRIAL_PACK,
} from '../data/pricing.ts';
import type { LlmBilling } from '../data/pricing.ts';

const trialPack = API_TIERS.find((t) => t.id === 'trial_pack')!;
const manualTiers = API_TIERS.filter((t) => t.tierType === 'manual');
const apiTiers = API_TIERS.filter((t) => t.tierType === 'api');

function fmtUsd(value: number | null | undefined): string {
  if (value === null || value === undefined) return '—';
  return value % 1 === 0 ? `$${value.toLocaleString('en-US')}` : `$${value.toFixed(2)}`;
}

function fmtAiAgent(aiAgent: boolean, llmBilling: LlmBilling): string {
  if (!aiAgent) return '—';
  switch (llmBilling) {
    case 'byok_only':
      return 'BYOK (Anthropic key required)';
    case 'byok_or_bundled':
      return 'BYOK or bundled (your choice)';
    case 'byok_or_bundled_custom':
      return 'BYOK or bundled (custom rate)';
    default:
      return '—';
  }
}

function fmtArchetypes(max: number | null): string {
  if (max === null) return 'All + custom';
  return max === 1 ? '1 archetype' : `${max.toString()} archetypes`;
}

function fmtSupportTier(tier: 'email_48h' | 'email_slack_12h' | 'dedicated_csm_1h'): string {
  switch (tier) {
    case 'email_48h':
      return 'Email · 48h SLA';
    case 'email_slack_12h':
      return 'Email + Slack Connect · 12h SLA';
    case 'dedicated_csm_1h':
      return 'Dedicated CSM · 1h SLA';
  }
}

function fmtCustomArchetypeDev(level: 'none' | 'limited' | 'unlimited'): string {
  switch (level) {
    case 'none':
      return '—';
    case 'limited':
      return 'Limited (1/yr)';
    case 'unlimited':
      return 'Unlimited';
  }
}

---

<BaseLayout
  title="Pricing"
  description="Driftstack pricing: $2.99 trial pack, then per-concurrent-session subscription across two ladders — Manual for humans clicking the GUI, API for code calling the SDK. Self-hosted for sovereignty."
>
  <!-- Header -->
  <section class="relative overflow-hidden">
    {/* 2026-05-22 — same hero-glow atmosphere as the landing page so
        the pricing surface reads as part of the same visual family. */}
    <div class="hero-glow" aria-hidden="true"></div>
    <div class="absolute inset-0 grid-bg opacity-40" aria-hidden="true"></div>
    <div class="relative mx-auto max-w-6xl px-6 py-24 animate-fade-up">
      <p class="section-label">Pricing</p>
      <h1
        class="mt-6 text-4xl font-semibold tracking-tight md:text-5xl lg:text-6xl"
        style="letter-spacing: -0.02em;"
      >
        
          Two ladders.
        
        <span class="block text-ink-primary">One trial pack to start.</span>
      </h1>
      <p class="mt-6 max-w-prose text-lg text-ink-secondary leading-relaxed">
        <strong class="text-ink-primary">Manual</strong> if a person is driving
        sessions in our desktop app.
        <strong class="text-ink-primary">API</strong> if your code is calling
        the SDK. Same engine underneath; pick the workflow that matches yours.
        Try the $2.99 trial pack first to see which feels right.
      </p>
    </div>
  </section>

  <!-- Trial pack hero card -->
  <section id="trial-pack" class="border-t border-white/10 bg-surface-base">
    <div class="mx-auto max-w-6xl px-6 py-16">
      <div
        class="grid gap-8 rounded-lg border border-glow-red/30 bg-surface-raised p-8 md:grid-cols-3 md:items-center"
      >
        <div class="md:col-span-2">
          {/* 2026-05-23 — icon + gradient text on trial-pack hero h2. */}
          <div class="flex items-start gap-3">
            <div class="flex h-10 w-10 shrink-0 items-center justify-center rounded-lg bg-glow-red/10 ring-1 ring-glow-red/30 text-glow-red mt-1">
              <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                <polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2" />
              </svg>
            </div>
            <div>
              <p class="font-mono text-xs uppercase tracking-widest text-glow-red">Start here</p>
              <h2 class="mt-3 text-2xl font-semibold text-ink-primary">
                  Trial pack — {fmtUsd(trialPack.monthlyUsd)}, one-time
                </h2>
            </div>
          </div>
          <p class="mt-3 max-w-prose text-ink-secondary">
            $2.99 buys ~{TRIAL_PACK.hoursApprox} hours of iPhone Safari
            sessions to evaluate the platform. {TRIAL_PACK.concurrent}
            concurrent session, {TRIAL_PACK.windowDays}-day window from
            purchase, used once per account.
          </p>
          <p class="mt-2 max-w-prose text-sm text-ink-muted">
            How the meter works: $2.99 of pre-paid credit, billed at
            {TRIAL_PACK.meterRate}. The trial is our only product that
            charges by the hour — paid tiers don't meter usage at all,
            just the number of sessions you run in parallel.
          </p>
          <ul class="mt-4 space-y-1 text-sm text-ink-secondary">
            <li class="flex gap-2">
              <span class="text-glow-red">•</span> Real iOS WebKit. No emulation, no runtime
              patches.
            </li>
            <li class="flex gap-2">
              <span class="text-glow-red">•</span> Same SDK + GUI as paid tiers — switch tiers without
              code changes.
            </li>
            <li class="flex gap-2">
              <span class="text-glow-red">•</span> Unused credit expires after {TRIAL_PACK.windowDays}
              days; subscribe to a tier to continue.
            </li>
          </ul>
        </div>
        <div class="flex flex-col gap-3 md:items-end">
          <a href="https://app.driftstack.dev/signup" class="btn-primary w-full md:w-auto"
            >Start for {fmtUsd(trialPack.monthlyUsd)}</a
          >
          <a href="https://docs.driftstack.dev" class="btn-secondary w-full md:w-auto"
            >Read the docs</a
          >
        </div>
      </div>
    </div>
  </section>

  <!-- Positioning band -->
  <section id="no-overages" class="border-t border-white/10 bg-surface-raised">
    <div class="mx-auto max-w-4xl px-6 py-16">
      <p class="text-2xl font-medium tracking-tight text-ink-primary md:text-3xl">
        Pay per concurrent session.
        <span class="text-ink-muted"
          >Run as many hours as you want within your concurrent cap.</span
        > No surprise overage bills.
      </p>
      <p class="mt-4 max-w-prose text-sm text-ink-secondary">
        <strong class="text-ink-primary">Concurrent sessions</strong> = how many
        sessions you can run at the same time, like browser tabs you'd have
        open at once. The number after each tier (e.g. "3 concurrent") is the
        cap; everything else (hours, calls, page navigations) is unmetered
        within the cap.
      </p>
    </div>
  </section>

  <!-- Monthly/annual toggle (controls both Manual + API tables) -->
  <!-- V-502 — decision-tree section between trial-pack hero and the
       monthly/annual toggle. Static prose ladder; intentionally
       no JS interaction (Tier-1 placeholder bound). -->
  <section
    id="which-tier"
    aria-label="Which tier is right for me?"
    class="border-t border-white/10 bg-surface-raised"
  >
    <div class="mx-auto max-w-6xl px-6 py-16">
      <p class="font-mono text-xs uppercase tracking-widest text-glow-red">
        Pick your tier
      </p>
      <h2 class="mt-3 text-2xl font-semibold tracking-tight text-ink-primary">Which tier is right for me?</h2>
      <p class="mt-3 max-w-3xl text-sm text-ink-secondary">
        All tiers run the same engine and the same fingerprint surface.
        Differences are concurrent caps + profile counts + AI-agent
        availability. The right tier is the one that matches your access
        pattern (Manual vs API) and your peak parallelism — switch any
        time, prorated automatically.
      </p>
      <p class="mt-3 text-sm text-ink-secondary">
        Need every dimension side-by-side?
        <a href="/pricing/comparison" class="font-medium text-glow-red underline"
          >See the per-tier comparison →</a
        >
      </p>

      <ul class="mt-8 grid gap-4 md:grid-cols-2">
        <li class="rounded-lg border border-glow-red/30 bg-surface-raised p-5">
          <p class="font-mono text-xs uppercase tracking-widest text-glow-red">
            Just looking
          </p>
          <h3 class="mt-2 font-medium text-ink-primary">
            Trial pack — $2.99
          </h3>
          <p class="mt-2 text-sm text-ink-secondary">
            16 hours of session time, no auto-renewal. The right entry if
            you want to run your existing test suite or scraping job
            against Driftstack and compare outputs before committing.
          </p>
        </li>
        <li class="rounded-lg border border-white/10 bg-surface-raised p-5">
          <p class="font-mono text-xs uppercase tracking-widest text-ink-muted">
            Solo operator, GUI
          </p>
          <h3 class="mt-2 font-medium text-ink-primary">Solo Manual — $79/mo</h3>
          <p class="mt-2 text-sm text-ink-secondary">
            One human clicking in the desktop GUI client; up to 10
            persistent profiles. Power users running 1 session at a time
            across 10 different client identities — sneaker resellers,
            sole-trader compliance work, individual researchers.
          </p>
        </li>
        <li class="rounded-lg border border-white/10 bg-surface-raised p-5">
          <p class="font-mono text-xs uppercase tracking-widest text-ink-muted">
            Small team, GUI
          </p>
          <h3 class="mt-2 font-medium text-ink-primary">Team Manual — $249/mo</h3>
          <p class="mt-2 text-sm text-ink-secondary">
            3 humans clicking in parallel; 50 profiles. Account managers
            running multiple client identities concurrently. AI-agent
            available (BYOK).
          </p>
        </li>
        <li class="rounded-lg border border-white/10 bg-surface-raised p-5">
          <p class="font-mono text-xs uppercase tracking-widest text-ink-muted">
            Agency, GUI
          </p>
          <h3 class="mt-2 font-medium text-ink-primary">Agency Manual — $699/mo</h3>
          <p class="mt-2 text-sm text-ink-secondary">
            8 concurrent operators; 200 profiles. Marketing agencies,
            compliance teams, legal-tech ops juggling many client
            identities at once.
          </p>
        </li>
        <li class="rounded-lg border border-white/10 bg-surface-raised p-5">
          <p class="font-mono text-xs uppercase tracking-widest text-ink-muted">
            Solo dev, code
          </p>
          <h3 class="mt-2 font-medium text-ink-primary">API Starter — $149/mo</h3>
          <p class="mt-2 text-sm text-ink-secondary">
            Programmatic SDK access; 2 concurrent sessions; 25 profiles.
            Solo developers writing test automation or evaluation-stage
            scrapers. AI-agent available (BYOK).
          </p>
        </li>
        <li class="rounded-lg border border-white/10 bg-surface-raised p-5">
          <p class="font-mono text-xs uppercase tracking-widest text-ink-muted">
            Production scale, code
          </p>
          <h3 class="mt-2 font-medium text-ink-primary">API Builder — $499/mo</h3>
          <p class="mt-2 text-sm text-ink-secondary">
            8 concurrent; 100 profiles. Production automation jobs,
            CI suites running iPhone Safari on real WebKit, AI-agent
            flows with either BYOK or bundled-rate billing.
          </p>
        </li>
        <li class="rounded-lg border border-white/10 bg-surface-raised p-5">
          <p class="font-mono text-xs uppercase tracking-widest text-ink-muted">
            High throughput, code
          </p>
          <h3 class="mt-2 font-medium text-ink-primary">API Scale — $1,499/mo</h3>
          <p class="mt-2 text-sm text-ink-secondary">
            24 concurrent; 500 profiles. High-volume automation fleets,
            agencies running scraping jobs across many domains, AI-agent
            workflows at scale.
          </p>
        </li>
        <li class="rounded-lg border border-white/10 bg-surface-raised p-5">
          <p class="font-mono text-xs uppercase tracking-widest text-ink-muted">
            Custom contract
          </p>
          <h3 class="mt-2 font-medium text-ink-primary">Enterprise — from $4,000/mo</h3>
          <p class="mt-2 text-sm text-ink-secondary">
            Annual contracts only. Custom concurrency + profiles +
            archetypes + SLA + DPA terms. The right tier for regulated
            industries (finance, healthcare) needing custom paperwork or
            customers needing a dedicated CSM.
          </p>
        </li>
      </ul>

      <p class="mt-8 max-w-3xl text-sm text-ink-secondary">
        Need to run on your own hardware (regulated industry, air-gapped
        deployment, custom hardware fingerprint)?
        <a href="#self-hosted" class="text-glow-red underline">
          See self-hosted SKUs
        </a>
        — pricing is engine-license-based with source-escrow, not the
        per-seat ladder above. Or
        <a href="/faq#architecture-sessions" class="text-glow-red underline">
          read the architecture FAQ
        </a>
        if you're still deciding whether real-WebKit fits your workload.
      </p>
    </div>
  </section>

  <section class="border-t border-white/10 bg-surface-base">
    <div class="mx-auto max-w-6xl px-6 py-10">
      <div class="flex items-center justify-end">
        <div
          id="billing-toggle"
          class="inline-flex rounded-md border border-white/10 bg-surface-raised p-1 text-sm font-medium"
          role="tablist"
        >
          <button
            type="button"
            data-period="monthly"
            class="billing-toggle-btn rounded-sm px-3 py-1.5 text-ink-secondary"
            aria-selected="true"
          >
            Monthly
          </button>
          <button
            type="button"
            data-period="annual"
            class="billing-toggle-btn rounded-sm px-3 py-1.5 text-ink-secondary"
            aria-selected="false"
          >
            Annual
            <span
              class="ml-1 rounded-sm bg-glow-red/20 px-1.5 py-0.5 font-mono text-[10px] uppercase tracking-wider text-glow-red"
              >−20%</span
            >
          </button>
        </div>
      </div>
    </div>
  </section>

  <!-- Manual ladder -->
  <section id="manual" class="bg-surface-base">
    <div class="mx-auto max-w-6xl px-6 pb-20">
      <div class="mb-10 flex items-start gap-4">
        <div class="flex h-10 w-10 shrink-0 items-center justify-center rounded-lg bg-glow-red/10 ring-1 ring-glow-red/30 text-glow-red mt-1">
          <svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <circle cx="12" cy="7" r="4" />
            <path d="M5 21v-2a4 4 0 0 1 4-4h6a4 4 0 0 1 4 4v2" />
          </svg>
        </div>
        <div>
          <p class="font-mono text-xs uppercase tracking-widest text-glow-red">Manual</p>
          <h2 class="mt-3 text-3xl font-semibold tracking-tight text-ink-primary">
              Manual — for humans
            </h2>
          <p class="mt-3 max-w-prose text-ink-secondary">
            Persistent profiles. Drive sessions yourself in the GUI client. No code
            required.
          </p>
        </div>
      </div>

      <div class="grid gap-6 md:grid-cols-3">
        {
          manualTiers.map((tier) => (
            <div
              class:list={[
                'rounded-lg border bg-surface-raised p-6',
                tier.highlight ? 'border-oxblood-700 shadow-md' : 'border-white/10',
              ]}
            >
              <div class="flex items-center justify-between">
                <h3 class="text-lg font-semibold text-ink-primary">{tier.name}</h3>
                {tier.highlight && (
                  <span class="rounded-sm bg-glow-red/20 px-1.5 py-0.5 font-mono text-[10px] uppercase tracking-wider text-glow-red">
                    popular
                  </span>
                )}
              </div>
              <div class="price-monthly mt-3" data-period-target="monthly">
                <span class="text-2xl font-semibold text-ink-primary">{fmtUsd(tier.monthlyUsd)}</span>
                <span class="text-sm text-ink-muted">/mo</span>
              </div>
              <div class="price-annual mt-3 hidden" data-period-target="annual">
                <span class="text-2xl font-semibold text-ink-primary">
                  {fmtUsd(tier.annualMonthlyEquivalentUsd)}
                </span>
                <span class="text-sm text-ink-muted">
                  /mo · billed {fmtUsd(tier.annualUsd)} yearly
                </span>
              </div>
              <dl class="mt-6 space-y-3 text-sm">
                <div class="flex justify-between gap-4 border-t border-white/10 pt-3">
                  <dt class="text-ink-muted">Browser profiles</dt>
                  <dd class="text-right font-medium text-ink-primary">{tier.profiles.toString()}</dd>
                </div>
                <div class="flex justify-between gap-4 border-t border-white/10 pt-3">
                  <dt class="text-ink-muted">Concurrent sessions</dt>
                  <dd class="text-right font-medium text-ink-primary">{tier.concurrent.toString()}</dd>
                </div>
                <div class="flex justify-between gap-4 border-t border-white/10 pt-3">
                  <dt class="text-ink-muted">Hours</dt>
                  <dd class="text-right text-ink-primary">{tier.hoursLabel}</dd>
                </div>
                <div class="flex justify-between gap-4 border-t border-white/10 pt-3">
                  <dt class="text-ink-muted">AI agent (LLM-driven sessions)</dt>
                  <dd class="text-right text-ink-secondary">
                    {fmtAiAgent(tier.aiAgent, tier.llmBilling)}
                  </dd>
                </div>
                <div class="flex justify-between gap-4 border-t border-white/10 pt-3">
                  <dt class="text-ink-muted">Support</dt>
                  <dd class="text-right text-ink-secondary">{tier.support}</dd>
                </div>
              </dl>
              <a href="https://app.driftstack.dev/signup" class="btn-primary mt-6 w-full">Start with $2.99</a>
              <a
                href={`https://app.driftstack.dev/signup?tier=${tier.id}`}
                class="mt-2 block w-full text-center text-sm font-medium text-glow-red underline-offset-4 hover:underline"
              >
                Buy {tier.name} →
              </a>
            </div>
          ))
        }
      </div>
    </div>
  </section>

  <!-- Visual divider -->
  <div class="bg-surface-raised">
    <div class="mx-auto max-w-6xl px-6">
      <div class="h-px bg-oxblood-700"></div>
    </div>
  </div>

  <!-- API ladder -->
  <section id="api" class="bg-surface-raised">
    <div class="mx-auto max-w-6xl px-6 py-20">
      <div class="mb-10 flex items-start gap-4">
        <div class="flex h-10 w-10 shrink-0 items-center justify-center rounded-lg bg-glow-red/10 ring-1 ring-glow-red/30 text-glow-red mt-1">
          <svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <polyline points="16 18 22 12 16 6" />
            <polyline points="8 6 2 12 8 18" />
          </svg>
        </div>
        <div>
          <p class="font-mono text-xs uppercase tracking-widest text-glow-red">API</p>
          <h2 class="mt-3 text-3xl font-semibold tracking-tight text-ink-primary">
              API — for code
            </h2>
        <p class="mt-3 max-w-prose text-ink-secondary">
          SDK in your language. Programmatic session creation. Concurrent caps that
          scale with your fleet.
        </p>
        <p class="mt-3 max-w-prose text-sm text-ink-muted">
          You'll see <strong class="text-ink-secondary">"BYOK"</strong> in the tier
          tables below — it means
          <em>bring your own API key</em> from Anthropic for the optional AI
          agent feature. Your model spend goes
          to your provider account; Driftstack doesn't markup or proxy.
          Bundled LLM is the alternative — Driftstack proxies + bills on one
          invoice. Detail below the tier tables.
        </p>
        </div>
      </div>

      <div class="grid gap-6 md:grid-cols-2 lg:grid-cols-4">
        {
          apiTiers.map((tier) => (
            <div
              class:list={[
                'rounded-lg border bg-surface-raised p-6',
                tier.highlight ? 'border-oxblood-700 shadow-md' : 'border-white/10',
              ]}
            >
              <div class="flex items-center justify-between">
                <h3 class="text-lg font-semibold text-ink-primary">{tier.name}</h3>
                {tier.highlight && (
                  <span class="rounded-sm bg-glow-red/20 px-1.5 py-0.5 font-mono text-[10px] uppercase tracking-wider text-glow-red">
                    popular
                  </span>
                )}
              </div>
              {tier.id === 'enterprise' ? (
                <div class="mt-3">
                  <span class="text-2xl font-semibold text-ink-primary">
                    from {fmtUsd(tier.annualUsd)}/mo
                  </span>
                  <div class="text-xs text-ink-muted">annual contract only</div>
                </div>
              ) : (
                <>
                  <div class="price-monthly mt-3" data-period-target="monthly">
                    <span class="text-2xl font-semibold text-ink-primary">
                      {fmtUsd(tier.monthlyUsd)}
                    </span>
                    <span class="text-sm text-ink-muted">/mo</span>
                  </div>
                  <div class="price-annual mt-3 hidden" data-period-target="annual">
                    <span class="text-2xl font-semibold text-ink-primary">
                      {fmtUsd(tier.annualMonthlyEquivalentUsd)}
                    </span>
                    <span class="text-sm text-ink-muted">
                      /mo · billed {fmtUsd(tier.annualUsd)} yearly
                    </span>
                  </div>
                </>
              )}
              <dl class="mt-6 space-y-3 text-sm">
                <div class="flex justify-between gap-4 border-t border-white/10 pt-3">
                  <dt class="text-ink-muted">Browser profiles</dt>
                  <dd class="text-right font-medium text-ink-primary">{tier.profiles.toString()}</dd>
                </div>
                <div class="flex justify-between gap-4 border-t border-white/10 pt-3">
                  <dt class="text-ink-muted">Concurrent sessions</dt>
                  <dd class="text-right font-medium text-ink-primary">{tier.concurrent.toString()}</dd>
                </div>
                <div class="flex justify-between gap-4 border-t border-white/10 pt-3">
                  <dt class="text-ink-muted">Hours</dt>
                  <dd class="text-right text-ink-primary">{tier.hoursLabel}</dd>
                </div>
                <div class="flex justify-between gap-4 border-t border-white/10 pt-3">
                  <dt class="text-ink-muted">AI agent (LLM-driven sessions)</dt>
                  <dd class="text-right text-ink-secondary">
                    {fmtAiAgent(tier.aiAgent, tier.llmBilling)}
                  </dd>
                </div>
                <div class="flex justify-between gap-4 border-t border-white/10 pt-3">
                  <dt class="text-ink-muted">Support</dt>
                  <dd class="text-right text-ink-secondary">{tier.support}</dd>
                </div>
              </dl>
              {
                tier.id === 'enterprise' ? (
                  <a href={tier.cta.href} class="btn-primary mt-6 w-full">
                    {tier.cta.label}
                  </a>
                ) : (
                  <>
                    <a href="https://app.driftstack.dev/signup" class="btn-primary mt-6 w-full">
                      Start with $2.99
                    </a>
                    <a
                      href={`https://app.driftstack.dev/signup?tier=${tier.id}`}
                      class="mt-2 block w-full text-center text-sm font-medium text-glow-red underline-offset-4 hover:underline"
                    >
                      Buy {tier.name} →
                    </a>
                  </>
                )
              }
            </div>
          ))
        }
      </div>

      <p class="mt-8 text-xs text-ink-muted">
        All prices in USD. VAT/BTW added per region per applicable EU rules. No
        setup fees on any tier. Annual contracts billed up front.
      </p>
    </div>
  </section>

  <!-- Visual divider -->
  <div class="bg-surface-base">
    <div class="mx-auto max-w-6xl px-6">
      <div class="h-px bg-oxblood-700"></div>
    </div>
  </div>

  <!-- Self-hosted ladder -->
  <section id="self-hosted" class="bg-surface-base">
    <div class="mx-auto max-w-6xl px-6 py-20">
      <div class="mb-10">
        <p class="font-mono text-xs uppercase tracking-widest text-glow-red">Self-hosted</p>
        <h2 class="mt-3 text-3xl font-semibold tracking-tight text-ink-primary">
          Self-hosted — for sovereignty
        </h2>
        <p class="mt-3 max-w-prose text-ink-secondary">
          Run the entire stack on your own hardware. No concurrent-session caps from
          us — your hardware is the cap. Driftstack licenses the software, you
          scale the fleet.
        </p>
      </div>

      <div class="grid gap-6 md:grid-cols-3">
        {
          SELF_HOSTED_SKUS.map((sku) => (
            <div class="rounded-lg border border-white/10 bg-surface-raised p-6 shadow-sm">
              <h3 class="text-lg font-semibold text-ink-primary">{sku.name}</h3>
              <div class="mt-3">
                {sku.monthlyUsd === null ? (
                  <div>
                    <span class="text-2xl font-semibold text-ink-primary">
                      from {fmtUsd(sku.annualMonthlyEquivalentUsd)}
                    </span>
                    <span class="text-sm font-normal text-ink-muted">/mo</span>
                    <div class="text-xs text-ink-muted">annual contract only</div>
                  </div>
                ) : (
                  <>
                    <div class="text-2xl font-semibold text-ink-primary">
                      {fmtUsd(sku.monthlyUsd)}
                      <span class="text-sm font-normal text-ink-muted">/mo</span>
                    </div>
                    <div class="text-xs text-ink-muted">
                      or {fmtUsd(sku.annualMonthlyEquivalentUsd)}/mo billed annually
                    </div>
                  </>
                )}
              </div>
              <dl class="mt-6 space-y-3 text-sm">
                <div class="flex justify-between gap-4 border-t border-white/10 pt-3">
                  <dt class="text-ink-muted">Browser profiles</dt>
                  <dd class="text-right font-medium text-ink-primary">
                    {sku.profilesMax === null ? 'Unlimited' : sku.profilesMax.toString()}
                  </dd>
                </div>
                <div class="flex justify-between gap-4 border-t border-white/10 pt-3">
                  <dt class="text-ink-muted">Archetypes</dt>
                  <dd class="text-right text-ink-primary">{fmtArchetypes(sku.archetypesMax)}</dd>
                </div>
                <div class="flex justify-between gap-4 border-t border-white/10 pt-3">
                  <dt class="text-ink-muted">Software updates</dt>
                  <dd class="text-right text-ink-secondary">{SELF_HOSTED_SOFTWARE_UPDATES[sku.id]}</dd>
                </div>
                <div class="flex justify-between gap-4 border-t border-white/10 pt-3">
                  <dt class="text-ink-muted">Archetype updates</dt>
                  <dd class="text-right text-ink-secondary">{SELF_HOSTED_ARCHETYPE_UPDATES[sku.id]}</dd>
                </div>
                <div class="flex justify-between gap-4 border-t border-white/10 pt-3">
                  <dt class="text-ink-muted">Source code access</dt>
                  <dd class="text-right text-ink-secondary">{SELF_HOSTED_SOURCE_ACCESS[sku.id]}</dd>
                </div>
                <div class="flex justify-between gap-4 border-t border-white/10 pt-3">
                  <dt class="text-ink-muted">Custom archetype dev</dt>
                  <dd class="text-right text-ink-secondary">
                    {fmtCustomArchetypeDev(sku.customArchetypeDevelopment)}
                  </dd>
                </div>
                <div class="flex justify-between gap-4 border-t border-white/10 pt-3">
                  <dt class="text-ink-muted">Source escrow</dt>
                  <dd class="text-right text-ink-secondary">{sku.sourceEscrow ? 'Yes' : 'No'}</dd>
                </div>
                <div class="flex justify-between gap-4 border-t border-white/10 pt-3">
                  <dt class="text-ink-muted">Support</dt>
                  <dd class="text-right text-ink-secondary">{fmtSupportTier(sku.supportTier)}</dd>
                </div>
                <div class="flex justify-between gap-4 border-t border-white/10 pt-3">
                  <dt class="text-ink-muted">Minimum term</dt>
                  <dd class="text-right text-ink-secondary">{sku.minimumTermMonths.toString()}-month</dd>
                </div>
              </dl>
              <p class="mt-4 text-xs text-ink-muted">
                Concurrent capacity is bounded by your hardware, not by license.
                Hardware procurement detail at{' '}
                <a href="/self-hosted" class="text-glow-red underline">
                  /self-hosted
                </a>
                .
              </p>
              <a href={sku.ctaHref} class="btn-primary mt-6 w-full">
                Contact sales
              </a>
            </div>
          ))
        }
      </div>
    </div>
  </section>

  <!-- BYOK / Bundled LLM explainer -->
  <section class="border-t border-white/10 bg-surface-raised">
    <div class="mx-auto max-w-4xl px-6 py-16">
      <p class="font-mono text-xs uppercase tracking-widest text-glow-red">AI agent feature</p>
      <h2 class="mt-3 text-2xl font-semibold text-ink-primary">BYOK or bundled — your call.</h2>
      <p class="mt-4 max-w-prose text-ink-secondary">
        BYOK means you bring your <strong>Anthropic API key</strong> from
        <a
          href="https://console.anthropic.com"
          class="text-glow-red underline"
          target="_blank"
          rel="noopener noreferrer">console.anthropic.com</a
        > and pay Anthropic directly. Bundled LLM (API Builder, API Scale, Enterprise)
        means we proxy and bill — token usage rolls up to your Driftstack invoice
        with a per-token markup. Bundled per-token rate announced at launch.
        Self-hosted SKUs are BYOK-only because we don't proxy LLM calls into
        customer hardware.
      </p>
    </div>
  </section>

  <!-- Mini FAQ teaser -->
  <section class="border-t border-white/10 bg-surface-base">
    <div class="mx-auto max-w-6xl px-6 py-16">
      <h2 class="text-2xl font-semibold text-ink-primary">Common questions</h2>
      <div class="mt-8 grid gap-8 md:grid-cols-2">
        <div>
          <h3 class="font-medium text-ink-primary">Manual or API — which one?</h3>
          <p class="mt-2 text-sm text-ink-secondary">
            Manual if a human will be clicking in the GUI client. API if your code
            will be calling the SDK. Same engine, same fingerprints, same fidelity
            — different access surface, different upgrade trigger.
          </p>
        </div>
        <div>
          <h3 class="font-medium text-ink-primary">Why concurrent caps and not hours?</h3>
          <p class="mt-2 text-sm text-ink-secondary">
            Hours metering breaks for manual users — an account manager running 3
            persistent profiles 8 hours a day generates 720 browser-hours/month
            and a surprise overage bill. Concurrent caps mean you only pay for how
            many sessions you can run simultaneously. Use them as much as you
            want.
          </p>
        </div>
        <div>
          <h3 class="font-medium text-ink-primary">Can I switch tiers mid-month?</h3>
          <p class="mt-2 text-sm text-ink-secondary">
            Yes. Stripe prorates the change automatically. New limits apply
            immediately on the next session-creation gate.
          </p>
        </div>
        <div>
          <h3 class="font-medium text-ink-primary">What happens when the trial pack runs out?</h3>
          <p class="mt-2 text-sm text-ink-secondary">
            Either the credit hits zero or 14 days pass. New session attempts
            return 402 Payment Required pointing at Stripe Checkout for a paid
            tier. Trial pack is once per account; to come back, subscribe.
          </p>
        </div>
      </div>
      <div class="mt-10">
        <a href="/faq" class="btn-secondary">See full FAQ</a>
      </div>
    </div>
  </section>

  <!-- Monthly/Annual toggle script -->
  <script is:inline>
    (function () {
      const root = document.getElementById('billing-toggle');
      if (!root) return;
      const buttons = root.querySelectorAll('.billing-toggle-btn');
      const monthlyEls = document.querySelectorAll('[data-period-target="monthly"]');
      const annualEls = document.querySelectorAll('[data-period-target="annual"]');

      function setPeriod(period) {
        buttons.forEach((btn) => {
          btn.setAttribute(
            'aria-selected',
            btn.getAttribute('data-period') === period ? 'true' : 'false',
          );
          if (btn.getAttribute('data-period') === period) {
            btn.classList.add('bg-oxblood-700', 'text-white');
            btn.classList.remove('text-ink-secondary');
          } else {
            btn.classList.remove('bg-oxblood-700', 'text-white');
            btn.classList.add('text-ink-secondary');
          }
        });
        monthlyEls.forEach((el) => el.classList.toggle('hidden', period !== 'monthly'));
        annualEls.forEach((el) => el.classList.toggle('hidden', period !== 'annual'));
      }

      buttons.forEach((btn) => {
        btn.addEventListener('click', () => {
          const period = btn.getAttribute('data-period');
          if (period) setPeriod(period);
        });
      });

      setPeriod('monthly');
    })();
  </script>
</BaseLayout>
